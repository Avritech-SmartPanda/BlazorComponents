@if (!Example.New && !Example.Updated)
{
    <Property365PanelMenuItem @bind-Expanded=@Expanded Text="@Example.Name" Path="@Example.Path" Icon="@Example.Icon">
        @ChildContent
    </Property365PanelMenuItem>
}
else
{
    <Property365PanelMenuItem @bind-Expanded=@Expanded Text="@Example.Name" Path="@Example.Path" Icon="@Example.Icon">
        <Template>
            @Example.Name
            <Property365Badge Text="Updated" Visible=@Example.Updated />
            <Property365Badge Text="New" Visible=@Example.New BadgeStyle="BadgeStyle.Success" />
        </Template>
        <ChildContent>@ChildContent</ChildContent>
    </Property365PanelMenuItem>
}
@code {
    [Parameter]
    public Example Example { get; set; }

    [Parameter]
    public RenderFragment ChildContent { get; set; }

    [Parameter]
    public bool Expanded { get; set; }

    [Parameter]
    public EventCallback<bool> ExpandedChanged { get; set; }
}